[package]
name = "packet_loss_monitor"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.60", features = ["cargo"] }
tokio = { version = "1.34.0", features = ["full"] }

[dev-dependencies]
assert_cmd = "2.0.12"
